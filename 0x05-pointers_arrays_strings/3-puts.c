#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string, followed by a new line.
 * @str: char pointer.
 * Return: nothing.
 */
void _puts(char *str)
{
	char arr[7] = "stdout";

	printf("%s\n", arr);
}
